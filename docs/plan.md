app/
├── page.tsx                         // Trúc
├── products/
│   ├── page.tsx                     // Trúc
│   └── [slug]/
│       └── page.tsx                 // Trúc
├── cart/
│   └── page.tsx                     // Trúc
├── blog/
│   └── page.tsx                     // Ngọc Mỹ
├── about/
│   └── page.tsx                     // Ngọc Mỹ
├── checkout-success/
│   └── page.tsx                     // Ngọc Mỹ
└── not-found.tsx                    // Ngọc Mỹ

components/
├── layout/
│   ├── Navbar.tsx                   // Chung, đã xong
│   ├── Footer.tsx                   // Chung, đã xong
│   └── Newsletter.tsx               // Chung, đã xong
├── ProductCard.tsx                  // Trúc
├── ProductGrid.tsx                  // Trúc
├── CollectionCard.tsx               // Trúc
├── CartItem.tsx                     // Trúc
├── BlogCard.tsx                     // Ngọc Mỹ
└── SectionTitle.tsx                 // Ngọc Mỹ / dùng chung

data/
├── products.ts                      // Trúc
├── collections.ts                   // Trúc
├── blogs.ts                         // Ngọc Mỹ
└── about.ts                         // Ngọc Mỹ